# better-sqlite3 ships prebuilt binaries for all supported platforms, so its
# node-gyp postinstall step is unnecessary and fails in environments without
# a C++ toolchain (e.g. no `make`). Skipping install scripts keeps `npm install`
# working everywhere.
ignore-scripts=true
